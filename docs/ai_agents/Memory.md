# Memory System

The memory subsystem provides vector-ready data primitives for:

- Conversation memory
- Project memory
- User memory
- Workflow memory
- Knowledge memory

The current implementation uses a normalized JSON storage model with future-ready semantic search hooks and a scope-based storage contract.
