import ClientPortalClient from "./ClientPortalClient";

export const metadata = {
  title: "Client Portal — Executive SEO & Link Performance",
  description: "White-label client portal for real-time SEO scores, indexing progress, outreach campaigns, and executive reports.",
};

export default function ClientPortalPage() {
  return <ClientPortalClient />;
}
